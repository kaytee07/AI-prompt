/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
  images : {
    domains : ['lh3.googleusercontent.com', 'localhost', 'picsum.photos'] // <== Domain name
  }
};

export default nextConfig;
